const request = require('request');
const request_promise = require('request-promise');

const getPubs = async (fields) => {
    const response = await request_promise('#');
}

module.exports = {
    getPubs
};